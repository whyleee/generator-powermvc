language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-aspnet-powermvc' ]; then cd .. && eval "mv $currentfolder generator-aspnet-powermvc" && cd generator-aspnet-powermvc; fi

